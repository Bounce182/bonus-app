<%= form_for(@calendar_line) do |f| %>
  <% if @calendar_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendar_line.errors.count, "error") %> prohibited this calendar_line from being saved:</h2>

      <ul>
      <% @calendar_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
